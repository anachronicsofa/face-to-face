<%= form_with(model: room, local: true) do |form| %>
  <% if room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(room.errors.count, "error") %> prohibited this room from being saved:</h2>

      <ul>
      <% room.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :room_name %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :room_user_id %>
  </div>

  <div class="field">
    <%= form.label :open %>
    <%= form.check_box :open, id: :room_open %>
  </div>

  <div class="field">
    <%= form.label :password %>
    <%= form.text_field :password, id: :room_password %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
